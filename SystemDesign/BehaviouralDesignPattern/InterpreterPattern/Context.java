package SystemDesign.BehaviouralDesignPattern.InterpreterPattern;

public class Context {
    // For this simple example, no variables are needed.
    // You could extend this later to support variables (like x = 10).
}
